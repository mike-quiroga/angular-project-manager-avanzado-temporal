import { AppCustomUsernameDirective } from './app-custom-username.directive';

describe('AppCustomUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCustomUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
