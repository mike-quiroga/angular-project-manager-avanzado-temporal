import { AppForDirective } from './app-for.directive';

describe('AppForDirective', () => {
  it('should create an instance', () => {
    // const directive = new AppForDirective();
    // expect(directive).toBeTruthy();
  });
});
